name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # Install-time scripts stay off: a pre/post-install hook in any
      # transitive package would run with the token this job holds.
      - run: npm ci --ignore-scripts

      - run: npm run typecheck

      - run: npm run build
